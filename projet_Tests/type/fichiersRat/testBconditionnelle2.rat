int* add(int* a, int b){
    int r = 0;
    r= ((*a) + b);
    return (&r);
}

test{
    int* r= (new int);
    int* px= null;
    int i=0;
    px=&i;
    while((*px)<4){
        r= add(r,0);
        *px= ((*px)+1);
    }
    
}