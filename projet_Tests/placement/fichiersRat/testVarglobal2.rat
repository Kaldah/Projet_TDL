static rat a= [3/4];
static rat* b= (&a);
static bool c= true;

int add (int x) {
    int a= denom(a); 
    int b= num((*b));
    return ((a+b)+2);
}

int infe(int * c){
    if(((num(a))< (*c))){
        return (*c);
    }else{
        return (num(a));
    }
}

test{
    int *px= (new int);
    int b = 1;
    if(c){
        *px= add(2);
    }else{
        *px= infe(&b);
    }
    
}